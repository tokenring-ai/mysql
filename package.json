{
  "name": "@tokenring-ai/mysql",
  "description": "MySQL database resources integration",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/database": "0.2.0",
    "mysql2": "^3.16.0"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "catalog:",
    "typescript": "catalog:"
  }
}
