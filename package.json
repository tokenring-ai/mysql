{
  "name": "@tokenring-ai/mysql",
  "description": "Dedicated provider for connecting agents to MySQL database instances.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "@tokenring-ai/database": "workspace:*",
    "zod": "^4.4.3"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "build": "tsc --noEmit",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "type": "module",
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}